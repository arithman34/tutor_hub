services:
  db:
    restart: unless-stopped
    ports: []

  redis:
    restart: unless-stopped
    ports: []

  api:
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"

  worker:
    restart: unless-stopped

  beat:
    restart: unless-stopped
